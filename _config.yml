title: "Vibeissue"
description: "Collega agenti AI e GitHub Issues su Windows, macOS e Linux — Cursor, Claude Code, Copilot e altro. CLI Python, zero PowerShell, zero gh CLI."
baseurl: ""
url: "https://vibeissue.github.io"

author:
  name: "Antonio Trento"
  email: "info@antoniotrento.net"
  bio: "Local AI & Automation"

og_image: "/assets/images/logo/favicon.svg"

paginate: 20
paginate_path: "/blog/page:num"

plugins:
  - jekyll-feed
  - jekyll-sitemap
  - jekyll-paginate

faq_en:
  - question: "What is Vibeissue?"
    answer: "Vibeissue is a lightweight skeleton that connects AI coding agents to GitHub Issues on Windows, macOS, and Linux. The agent runs a Python CLI to create, comment, and close issues — no gh CLI, no PowerShell scripts. Works with Cursor, Claude Code, Copilot, and any agent that can read repo instructions and run shell commands."
  - question: "Is it Cursor-only?"
    answer: "No. Cursor is the best-tested path (.cursor/rules/github-issues.mdc), but the same CLI works with Claude Code (CLAUDE.md + AGENTS.md), GitHub Copilot (.github/copilot-instructions.md), Windsurf, Cline, Gemini CLI, Codex CLI, and manual use. See the Platforms page or the MkDocs guide at /docs/platforms/overview/."
  - question: "Do I need the gh CLI?"
    answer: "No. Vibeissue uses a stdlib-only Python script (urllib) that talks directly to the GitHub REST API. You only need Python and a Personal Access Token with Issues read/write."
  - question: "How does the agent know to use it?"
    answer: "Copy the skeleton into your repo (or use adopt.py). Each platform reads an instruction file: AGENTS.md (universal), .cursor/rules/ for Cursor, CLAUDE.md for Claude Code, .github/copilot-instructions.md for Copilot. The rule tells the agent to run python scripts/github_issue.py directly."
  - question: "Can I adopt it into an existing project?"
    answer: "Yes. Run python scripts/adopt.py --target ../my-repo --repo myorg/my-app. It copies the CLI, labels, issue templates, and agent instruction files for multiple platforms, then writes tracker/github_repo with your slug."
  - question: "Where does the token live?"
    answer: "Set GITHUB_TOKEN as an environment variable (Windows, macOS, Linux). Alternatives: .env (gitignored) or .secrets/github_token. On Windows, the CLI can also read user env vars from the registry if the IDE terminal does not inherit them. Never commit tokens or paste them in chat."

faq_it:
  - question: "Cos'è Vibeissue?"
    answer: "Vibeissue è uno skeleton leggero che collega agenti AI di coding a GitHub Issues su Windows, macOS e Linux. L'agente esegue una CLI Python per creare, commentare e chiudere issue — senza gh CLI e senza script PowerShell. Funziona con Cursor, Claude Code, Copilot e qualsiasi agente che legga istruzioni nel repo ed esegua comandi shell."
  - question: "Funziona solo con Cursor?"
    answer: "No. Cursor è il percorso più testato (.cursor/rules/github-issues.mdc), ma la stessa CLI funziona con Claude Code (CLAUDE.md + AGENTS.md), GitHub Copilot (.github/copilot-instructions.md), Windsurf, Cline, Gemini CLI, Codex CLI e uso manuale. Vedi la pagina Piattaforme o la guida MkDocs su /docs/it/platforms/overview/."
  - question: "Serve la gh CLI?"
    answer: "No. Vibeissue usa uno script Python solo stdlib (urllib) che parla direttamente con l'API REST di GitHub. Ti basta Python e un Personal Access Token con permessi Issues read/write."
  - question: "Come fa l'agente a usarlo?"
    answer: "Copia lo skeleton nel repo (o usa adopt.py). Ogni piattaforma legge un file istruzioni: AGENTS.md (universale), .cursor/rules/ per Cursor, CLAUDE.md per Claude Code, .github/copilot-instructions.md per Copilot. La regola obbliga l'agente a eseguire python scripts/github_issue.py da solo."
  - question: "Posso adottarlo in un progetto esistente?"
    answer: "Sì. Esegui python scripts/adopt.py --target ../my-repo --repo myorg/my-app. Copia CLI, label, template issue e file istruzioni per più piattaforme, poi scrive tracker/github_repo con il tuo slug."
  - question: "Dove metto il token?"
    answer: "Imposta GITHUB_TOKEN come variabile d'ambiente (Windows, macOS, Linux). Alternative: .env (gitignored) o .secrets/github_token. Su Windows la CLI può leggere anche le variabili utente dal registry se il terminale IDE non le eredita. Mai committare token o incollarli in chat."

social:
  github: "https://github.com/vibeissue/vibeissue"
  twitter: "https://x.com/antoniotrento"

cta:
  github_url: "https://github.com/vibeissue/vibeissue"
  docs_url: "https://vibeissue.github.io/docs/"
  platforms_url_en: "https://vibeissue.github.io/docs/platforms/overview/"
  platforms_url_it: "https://vibeissue.github.io/docs/it/platforms/overview/"
  get_started_it: "/it/get-started/"
  get_started_en: "/en/get-started/"

# MkDocs i18n (folder): English at /docs/, Italian at /docs/it/
docs:
  base_en: "/docs/"
  base_it: "/docs/it/"

popup:
  url: "https://github.com/vibeissue/vibeissue"
  en:
    url: "/en/get-started/"
    title: "Lost a bug in chat again?"
    body: "Wire any AI agent to GitHub Issues in minutes. Clone, token, labels — Cursor, Claude Code, Copilot, and more."
    button: "Get started"
  it:
    url: "/it/get-started/"
    title: "Hai perso un bug in chat?"
    body: "Collega qualsiasi agente AI a GitHub Issues in pochi minuti. Clone, token, label — Cursor, Claude Code, Copilot e altro."
    button: "Come iniziare"

permalink: "/:categories/:title/"
markdown: kramdown
highlighter: rouge

include:
  - assets
  - robots.txt


keywords:
  - vibe coding
  - ai coding agent
  - cursor
  - claude code
  - github copilot
  - github issues
  - issue tracker
  - python cli
  - developer workflow
  - bug tracking
  - task management

exclude:
  - docs/overrides
